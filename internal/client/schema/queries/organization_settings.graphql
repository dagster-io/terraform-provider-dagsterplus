query GetOrganizationSettings {
  organizationSettings {
    settings
  }
}

mutation SetOrganizationSettings($settings: GenericScalar) {
  setOrganizationSettings(organizationSettings: {settings: $settings}) {
    __typename
    ... on OrganizationSettings {
      settings
    }
  }
}
