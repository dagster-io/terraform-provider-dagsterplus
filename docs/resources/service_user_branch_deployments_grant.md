---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "dagsterplus_service_user_branch_deployments_grant Resource - dagsterplus"
subcategory: ""
description: |-
  Manages a permission grant for a Dagster+ service user across all branch deployments of a specific parent (full) deployment.
---

# dagsterplus_service_user_branch_deployments_grant (Resource)

Manages a permission grant for a Dagster+ service user across all branch deployments of a specific parent (full) deployment.

## Example Usage

```terraform
resource "dagsterplus_service_user_branch_deployments_grant" "ci_bot_prod_branches" {
  service_user_id   = dagsterplus_service_user.ci_bot.id
  parent_deployment = "prod"
  grant             = "EDITOR"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `parent_deployment` (String) The name of the full (parent) deployment whose branch deployments this grant applies to.
- `service_user_id` (String) The ID of the service user to grant access.

### Optional

- `custom_role_id` (String) The ID of a custom role to assign. Exactly one of grant or custom_role_id must be set.
- `grant` (String) Standard permission level. Exactly one of grant or custom_role_id must be set. Valid values: `VIEWER`, `LAUNCHER`, `EDITOR`, `ADMIN`.

### Read-Only

- `id` (String) Unique identifier in the form `{service_user_id}/{parent_deployment}`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import using {service_user_id}/{parent_deployment}.
terraform import dagsterplus_service_user_branch_deployments_grant.example <service_user_id>/<parent_deployment_name>
```
