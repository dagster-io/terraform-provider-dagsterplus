---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "dagsterplus_user_token Resource - dagsterplus"
subcategory: ""
description: |-
  Manages a Dagster+ user token. The token value is only available at creation time; it cannot be recovered after import. Changing the name forces a new resource.
---

# dagsterplus_user_token (Resource)

Manages a Dagster+ user token. The token value is only available at creation time; it cannot be recovered after import. Changing the name forces a new resource.

## Example Usage

```terraform
resource "dagsterplus_user_token" "ci" {
  name = "ci-pipeline-token"
}

output "user_token_value" {
  value     = dagsterplus_user_token.ci.token
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The label for the user token. Changing this forces a new resource.

### Read-Only

- `id` (String) The token ID assigned by Dagster+.
- `token` (String, Sensitive) The user token value. Only populated on creation; not recoverable after import.
- `user_id` (String) The ID of the user who owns this token. Required for revocation.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# User tokens are imported using the token ID assigned by Dagster+.
# The token ID can be found via the Dagster+ API (ListUserTokens query).
# IMPORTANT: The token value (secret) is only available at creation time and
# cannot be recovered after import. The `token` attribute will be empty after
# import — plan accordingly (e.g. use ignore_changes or recreate the token).
terraform import dagsterplus_user_token.example <token_id>
```
