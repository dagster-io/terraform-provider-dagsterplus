---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "dagsterplus_agent_token_deployment_grant Resource - dagsterplus"
subcategory: ""
description: |-
  Grants an agent token the AGENT permission on a specific deployment. Agent tokens only ever carry the AGENT permission, so there is no grant level to configure.
---

# dagsterplus_agent_token_deployment_grant (Resource)

Grants an agent token the AGENT permission on a specific deployment. Agent tokens only ever carry the AGENT permission, so there is no grant level to configure.

## Example Usage

```terraform
# Grant an agent token the AGENT permission on individual deployments.
# Useful when deployments are managed in a separate module from the token
# (e.g. iterating over deployments with for_each).
resource "dagsterplus_agent_token" "ecs_agent" {
  name         = "ecs-agent-token"
  organization = false
}

resource "dagsterplus_agent_token_deployment_grant" "prod" {
  agent_token_id = dagsterplus_agent_token.ecs_agent.id
  deployment     = "prod"
}

resource "dagsterplus_agent_token_deployment_grant" "staging" {
  agent_token_id = dagsterplus_agent_token.ecs_agent.id
  deployment     = "staging"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `agent_token_id` (String) The ID of the agent token to grant access. Changing this forces a new resource.
- `deployment` (String) The name of the deployment to grant access to. Changing this forces a new resource.

### Read-Only

- `id` (String) Unique identifier in the form `{agent_token_id}/{deployment}`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import using {agent_token_id}/{deployment}
terraform import dagsterplus_agent_token_deployment_grant.prod <agent_token_id>/prod
```
