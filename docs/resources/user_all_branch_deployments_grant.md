---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "dagsterplus_user_all_branch_deployments_grant Resource - dagsterplus"
subcategory: ""
description: |-
  Manages a permission grant for a Dagster+ user across all branch deployments.
---

# dagsterplus_user_all_branch_deployments_grant (Resource)

Manages a permission grant for a Dagster+ user across all branch deployments.

## Example Usage

```terraform
resource "dagsterplus_user_all_branch_deployments_grant" "alice" {
  user_id = dagsterplus_user.alice.id
  grant   = "LAUNCHER"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `user_id` (String) The ID of the user to grant access.

### Optional

- `custom_role_id` (String) The ID of a custom role to assign. Conflicts with grant.
- `grant` (String) Standard permission level. Conflicts with custom_role_id. Valid values: `VIEWER`, `LAUNCHER`, `EDITOR`, `ADMIN`.

### Read-Only

- `id` (String) Unique identifier (the user_id).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import an all-branch-deployments grant for a user using the user ID.
terraform import dagsterplus_user_all_branch_deployments_grant.example <user_id>
```
